# ServiceNow MCP Bridge - Environment Variables Example
# Copy this file to .env and fill in your credentials
# These variables override config.json settings (highest precedence)

# ===== REQUIRED: ServiceNow Instance =====
# Your ServiceNow instance domain (without https://)
# Example: dev12345.service-now.com
SERVICENOW_INSTANCE=your-instance.service-now.com

# ===== REQUIRED: Authentication =====
# Choose ONE authentication method below

# --- Option 1: Basic Authentication (username/password) ---
SERVICENOW_AUTH_TYPE=basic
SERVICENOW_USERNAME=your-username
SERVICENOW_PASSWORD=your-password

# --- Option 2: OAuth 2.0 (uncomment to use) ---
# SERVICENOW_AUTH_TYPE=oauth
# SERVICENOW_CLIENT_ID=your-client-id
# SERVICENOW_CLIENT_SECRET=your-client-secret
# SERVICENOW_TOKEN_URL=https://your-instance.service-now.com/oauth_token.do

# --- Option 3: Token Authentication (uncomment to use) ---
# SERVICENOW_AUTH_TYPE=token
# SERVICENOW_TOKEN=your-api-token

# ===== OPTIONAL: ServiceNow Client Settings =====
# These override config.json if set

# Timeout for API requests in milliseconds (default: 30000)
# SERVICENOW_TIMEOUT=30000

# Maximum retry attempts for failed requests (default: 3)
# SERVICENOW_MAX_RETRIES=3

# ===== OPTIONAL: Module Configuration =====
# Enable/disable specific modules (default: all enabled in read-only mode)

# Generic module (raw table access)
# MODULES_GENERIC_ENABLED=true
# MODULES_GENERIC_ALLOW_WRITE=false

# Knowledge Base module
# MODULES_KNOWLEDGE_ENABLED=true
# MODULES_KNOWLEDGE_ALLOW_WRITE=false

# Incident Management module
# MODULES_INCIDENT_ENABLED=true
# MODULES_INCIDENT_ALLOW_WRITE=false

# Change Management module (not yet implemented)
# MODULES_CHANGE_ENABLED=false
# MODULES_CHANGE_ALLOW_WRITE=false

# Problem Management module (not yet implemented)
# MODULES_PROBLEM_ENABLED=false
# MODULES_PROBLEM_ALLOW_WRITE=false

# CMDB module (not yet implemented)
# MODULES_CMDB_ENABLED=false
# MODULES_CMDB_ALLOW_WRITE=false

# Service Catalog module (not yet implemented)
# MODULES_CATALOG_ENABLED=false
# MODULES_CATALOG_ALLOW_WRITE=false

# User Management module (not yet implemented, always read-only)
# MODULES_USER_ENABLED=false

# ===== OPTIONAL: Rate Limiting =====
# Maximum API requests per hour (default: 1000)
# RATE_LIMIT_MAX_PER_HOUR=1000

# Burst size for short bursts of requests (default: 20)
# RATE_LIMIT_BURST_SIZE=20

# ===== OPTIONAL: Circuit Breaker =====
# Enable circuit breaker for failure protection (default: false)
# CIRCUIT_BREAKER_ENABLED=false

# Number of consecutive failures before opening circuit (default: 5)
# CIRCUIT_BREAKER_FAILURE_THRESHOLD=5

# Time in milliseconds before attempting to close circuit (default: 30000)
# CIRCUIT_BREAKER_RESET_TIMEOUT_MS=30000

# ===== OPTIONAL: Logging =====
# Logging level (default: info)
# Options: trace, debug, info, warn, error, fatal
# LOGGING_LEVEL=info

# ===== SECURITY NOTES =====
# - NEVER commit the .env file to version control
# - Use read-only mode (allow_write=false) by default
# - Enable write operations only for modules that require it
# - Use dedicated ServiceNow accounts with minimal permissions
# - Consider OAuth 2.0 for production deployments
